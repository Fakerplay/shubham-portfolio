# Animation improvement plans

Generated by `improve-animations` at commit `fcd47cc`. 9 audited components (`ColorPicker.tsx`, `ProceduralShadowOverlay.tsx`, `TerminalTypewriter.tsx`, `HoverReveal.tsx`, `ExpertisePillar.tsx`, `ExpertiseStack.tsx`, `ServicesAccordion.tsx`, `ServicesGrid.tsx`, `ServicesMarquee.tsx`) are confirmed dead code — not imported anywhere in the live app — so no plan touches them.

| # | Title | Severity | Status |
| --- | --- | --- | --- |
| [001](001-fix-reduced-motion-invisible-cards.md) | Fix reduced-motion invisible project cards | HIGH | DONE |
| [002](002-remove-always-on-wobble-animations.md) | Stop always-on wobble animations | HIGH | DONE |
| [003](003-footer-card-physicality.md) | Footer card entrance scale + exit easing | HIGH | DONE |
| [004](004-replace-transition-all.md) | Replace `transition-all` sitewide | MEDIUM | DONE |
| [005](005-button-press-feedback.md) | Add button press feedback sitewide | MEDIUM | DONE |
| [006](006-shared-easing-constants.md) | Shared easing constants (`lib/motion.ts`) | MEDIUM | DONE |
| [007](007-expandablebio-button-duration.md) | Tighten ExpandableBio button durations | MEDIUM | DONE |
| [008](008-header-hardware-acceleration.md) | Hardware-accelerate header scroll animation | MEDIUM | DONE |
| [009](009-interruptible-hero-word-animations.md) | Interruptible hero word animations | MEDIUM | DONE |
| [010](010-themedropdown-transform-origin.md) | ThemeDropdown transform-origin | LOW-MEDIUM | DONE |
| [011](011-gsap-stagger-and-orphaned-selector.md) | Tighten GSAP stagger + remove orphaned selector | LOW | DONE |
| [012](012-work-page-filter-stagger.md) | Work-page filter stagger | LOW | DONE |
| [013](013-delight-transitions.md) | Missed-opportunity delight transitions | LOW | DONE |
| [014](014-theme-control-delights-and-tactile-press.md) | Theme control delights, tactile press & header cleanup | MEDIUM | DONE |

## Recommended execution order

1. **001** first (correctness bug, touches `.featured-project-card`/`.lab-item-card` selectors that 011 also touches).
2. **011** right after 001 (both touch the same GSAP block in `app/page.tsx`).
3. **006** before 003/004/005 touch the same `ease`/`className` strings, to avoid rework — but not strictly required, since 003/004/005 note the coordination inline.
4. **002, 003, 004, 005, 007, 008, 009, 010, 012, 013** — no interdependencies beyond what's noted in each plan's Boundaries section; safe in any order.

## Dependencies between plans

- **001 ↔ 011**: both edit the same `gsap.fromTo`/`sections` block in `app/page.tsx`. Executed 001 then 011 in this pass.
- **003 ↔ 006**: 003's target easing value `[0.23, 1, 0.32, 1]` is identical to 006's `EASE_OUT` constant — if 006 lands first, 003 should import and use `EASE_OUT` instead of inlining the array.
- **004 ↔ 005**: both touch `className` transition-property strings on overlapping elements (e.g. `Header.tsx` ribbed grips, `ThemeLamp.tsx`, `ThemeDropdown.tsx`) — executed together in this pass to land one coherent property list per element.
